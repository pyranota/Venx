/**
 * This module stores any attribute information for voxel.
 *
 * What it does and how fast?
 *
 * Accessing block attribute at any index: O(1)
 * Removing block attribute: O(?)
 * Adding block attribute: O(?)
 *
 * ???Accessing blocks attribute for some leng: O(1)
 *
 * For example:
 * Traversing tree
 */

fn placeholder() {}
