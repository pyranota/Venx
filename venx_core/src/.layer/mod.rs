/// Used to extend original Plat struct
/// Importing it in venx makes plats extended and ready to use for venx
/// Importing it in venx shaders allows to use entire functionality of core directly in shaders
mod interfaces;
